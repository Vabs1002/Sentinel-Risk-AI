fastapi>=0.110.0
pydantic>=2.6.0
python-multipart>=0.0.9
uvicorn>=0.29.0

# Optional: LLM-powered dispute rebuttal generation
# Set GEMINI_API_KEY environment variable to activate.
# Without it, the Agentic RAG agent falls back to structured template output.
# Free tier at https://aistudio.google.com/app/apikey
# google-generativeai>=0.5.0
